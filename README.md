# Заметки на фикс в будущем для производительности и не только
- HelpItem, SecurityItem и в будущем еще один item имеют один и тот же интерфейс может их можно обьединить, а также индексы в Security компоненте?